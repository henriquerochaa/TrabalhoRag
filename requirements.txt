# --- Extração de PDF ---
PyMuPDF==1.27.2.3

# --- Chunking e NLP ---
spacy==3.8.14
# modelo pt_core_news_lg instalado separadamente: python -m spacy download pt_core_news_lg

# --- Embeddings e reranker (HuggingFace) ---
sentence-transformers==5.5.0
transformers==5.8.0
# torch: sentence-transformers instala automaticamente a versão compatível.
# Para CPU puro (professor/avaliador sem GPU) o download padrão inclui libs CUDA
# desnecessárias (~2 GB). Alternativa leve (~200 MB):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Deixamos sem pin de versão para garantir compatibilidade entre ambientes.
torch>=2.0.0

# --- Índice vetorial ---
# faiss-cpu==1.7.4 não está mais disponível no PyPI (versão mínima atual: 1.8.0)
# faiss-cpu==1.8.0 requer AVX-512 (inexistente no i5-13500 do professor)
# faiss-cpu==1.9.0: primeira versão compatível com numpy>=2.0 e sem exigência de AVX-512
faiss-cpu==1.9.0

# --- Armazenamento (SQLite via stdlib) ---
# sqlite3 é built-in do Python — sem dependência extra

# --- API ---
fastapi==0.136.1
pydantic==2.13.4
uvicorn[standard]>=0.29.0

# --- Interface Streamlit ---
streamlit>=1.35.0

# --- Configuração ---
PyYAML==6.0.3

# --- Utilitários ---
numpy==2.4.4

# --- Testes ---
pytest==9.0.3